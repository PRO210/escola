
//
Git:    Git:    Git:    Git:    Git:    Git:
//
git config --global user.name "Andre Freitas da Silva"
git config --global user.email "proandre21@hotmail.com"
git config --global color.ui true
//
mkdir EscolaGit (Criar Pastas)
cd Escola
git init
git status

git add "Seu Arquivo"
git add . (Adiciona todos os Arquivos)

git commit -m "Comitando o Código depois de um Hiato"
//
git log
git log -p (Mostra a difenrença entre os arquivos Antigo/Novo)
git log -p -2 (Difenrença entre os arquivos os dois últimosAntigo/Novo)
git log --stat (Resumo do que foi mudado)
git log --pretty=oneline (Comites em Linhas)
git log --pretty=format:"%h -%an, %ar : %s" (Comites em Linha pelo tempo)
git log --since=2.days (dois dias atrás)
//
kate .gitignore     (Negar arquivos)
//
git reset HEAD (Voltar o arquivo )
git reset HEAD~1 --soft (Removes os aquivos e deixa-os prontos para comitar)
git reset HEAD~1 --hard(Reoves os arquivos e os comites)
//
git checkout "Digite o Hasch" (voltar o arquivo para versão anterior )
git branch (Saber qual o branch que vc está)
git checkout master
git checkout -b (Criar Branch)
//
git merge "branch que vc quer adicionar"
PArei na aula 12 ou 13
//
//
ssh-keygen (gera chave)
cd ~/.ssh/  
cat id_rsa.pub
//
git remote add origin https://github.com/PRO210/Escola.git
kate .git/config
git push -u origin master (subir os arquivos)
PRO210
asdfg197346
//
git remote (Lista os seus repositórios remotos)
git clone "url do repositorio"
proandre21@hotmail.com
asdfg197346
git branch -a (Branchs locais e remotos)
git branch -b "endereço do branch remoto"
git pull nome do repositorio nome do branch (Baixa os arquivos do github)
git pull servertabeliao master
git push origin master (atualiza todos os arquivos) (Deve ser usado na máquina local)

jobsontenorio.com.br
jobso495
arkham495
ssh jobso495@jobsontenorio.com.br -p2222
cd /home/jobso495/proandre.com.br/Escola
//
//
git init --bare
ls
git remote add myserver ssh
cd hooks/
kate post-receive (Cria o arquivo)
#!/bin/sh
GIT_WORK_TREE=
chmod +x post-receive
